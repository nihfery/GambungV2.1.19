import { CreateConnectorFn } from 'wagmi';
import { CoinbaseWalletParameters } from '@wagmi/connectors';
import { EthereumProviderOptions as FamilyOptions } from 'family';
type DefaultConnectorsProps = {
    app: {
        name: string;
        icon?: string;
        description?: string;
        url?: string;
    };
    walletConnectProjectId?: string;
    coinbaseWalletPreference?: CoinbaseWalletParameters<'4'>['preference'];
    enableFamily?: boolean;
    familyOptions?: FamilyOptions;
};
declare const defaultConnectors: ({ app, walletConnectProjectId, coinbaseWalletPreference, enableFamily, familyOptions, }: DefaultConnectorsProps) => CreateConnectorFn[];
export default defaultConnectors;
